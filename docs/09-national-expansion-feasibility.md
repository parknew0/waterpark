# 전국 확장 타당성 검증

> 확인일: 2026-08-22
>
> 상태: `FACT` — 서울·경북 실측 완료. 나머지 14개 시도는 원본 미확보.
>
> 질문: 경북만으로는 지하주차장 침수 학습이 불가능했다. 전국으로 넓히면 성립하는가?

## 1. 결론

**성립한다.** 서울 하나만으로 경북 전체보다 양성 표본이 54배 많다.

| 항목 | 경북 | 서울 | 배수 |
| --- | ---: | ---: | ---: |
| 침수흔적 Polygon | 1,402건 | 9,002건 | 6.4배 |
| 지하층 보유 건물 | 25,340동 | 377,157동 | 14.9배 |
| **침수 Polygon 내부** | **175동** | **9,498동** | **54배** |
| 지하층 건물 중 침수 비율 | 0.69% | 2.52% | 3.7배 |

건물 수만 늘어난 것이 아니라 **침수 비율 자체가 3.7배 높다.** 이는 [6절 침수 위험 산출](./06-flood-risk-modeling.md)이 지적한 근본 원인, 즉 경북 침수흔적도가 농경지·하천 중심이라 도심 지하 구조물과 겹치지 않는다는 문제가 서울에서는 발생하지 않는다는 뜻이다.

## 2. 막혔던 원인과 해결

`FACT` [8절 감사](./08-national-data-and-code-audit.md) 3절이 확인한 대로, 먼저 받은 전국 VWorld 2022-12-03 스냅샷은 `A0~A22` 23필드뿐이라 `A27 지하층수`가 없었다. 지하주차장 판정의 1단계가 이 필드에서 시작하므로 전국 확장이 불가능했다.

`FACT` 원인은 스키마 개정 시점이다. VWorld는 기준일 2023-08-08부터 좌표계를 EPSG:5186으로 바꾸면서 필드도 늘렸다. 실측한 필드 수는 다음과 같다.

| 스냅샷 | 필드 수 | 범위 | A27 | CRS |
| --- | ---: | --- | --- | --- |
| 전국 2022-12-03 | 23 | `A0~A22` | 없음 | EPSG:5174 |
| 경북 2023-12-04 | 24 | — | — | EPSG:5186 |
| 경북 2025-12-04 | 29 | `A0~A28` | 있음 | EPSG:5186 |
| **서울 2026-08-09** | **29** | `A0~A28` | **있음** | **EPSG:5186** |

`FACT` 따라서 해결책은 별도 자료를 찾는 것이 아니라 **최신 기준일 스냅샷을 받는 것**이었다. 2026-08-09 스냅샷은 A27을 포함한다.

`FACT` 2026-08-09 전체데이터는 전국 통합 파일이 아니라 16개 시도별 파일로 제공된다. 전남과 광주는 `전남광주통합특별시` 한 묶음이다. 합계 약 2,196 MB다.

## 3. 검증 방법과 재현성

`FACT` 산출은 [`scripts/analyze_basement_flood_overlap.py`](../scripts/analyze_basement_flood_overlap.py)로 한다. 판정 기준은 [`build_flood_training_table.py`](../scripts/build_flood_training_table.py)와 동일하게 맞췄다.

```text
건물  = 건물 Polygon의 representative point (오목한 건물에서 중심점이 밖으로 나가는 것을 피함)
침수  = 그 점이 같은 시도의 침수 Polygon 합집합 안에 있음
좌표  = 원본 EPSG:5186 → EPSG:4326 변환 후 판정
```

`FACT` 원본 도형은 수정하지 않는다. 유효하지 않은 Polygon은 메모리상 합집합을 만들 때만 `make_valid`로 복구한다.

### 스크립트 신뢰성 검증

`FACT` 같은 스크립트를 경북 2025-12-04에 돌려 기존 파이프라인 결과를 재현했다.

| 값 | 기존 파이프라인 | 이 스크립트 | 판정 |
| --- | ---: | ---: | --- |
| 지하층 보유 건물 | 25,336동 | 25,340동 | 일치 (차이 4동) |
| 침수 Polygon 내부 | 176동 | 175동 | 일치 (차이 1동) |
| 비율 | 0.69% | 0.69% | 일치 |

차이 4동은 기존 특징표가 건축물대장 조인 단계에서 일부 행을 잃은 결과이며, GIS 후보 원본 25,340동과는 정확히 일치한다. 따라서 서울 9,498동도 같은 기준으로 산출된 값이다.

## 4. 중요한 한계: 9,498동이 전부 지하주차장은 아니다

`FACT` 서울에서 침수된 지하층 건물의 용도 분포다.

| 용도 | 건물 수 | 비율 |
| --- | ---: | ---: |
| 단독주택 | 6,379 | 67.2% |
| 제2종근린생활시설 | 988 | 10.4% |
| 제1종근린생활시설 | 963 | 10.1% |
| 공동주택 | 882 | 9.3% |
| 업무시설 | 111 | 1.2% |
| 그 외 | 175 | 1.8% |

**3분의 2가 단독주택이다.** 이는 지하주차장이 아니라 반지하 주택으로 보아야 한다. 서울 침수흔적도가 도심 자료라는 것은 확인됐지만, 그 도심 침수의 주된 피해 대상은 지하주차장이 아니라 반지하 거주공간이다.

### 지하주차장 확정 기대치

`FACT` 경북에서 실제로 측정한 용도별 지하주차장 확정률이다. 지하층이 있다고 해서 주차장인 것은 아니다.

| 용도 | 지하층 건물 | 주차 확정 | 확정률 |
| --- | ---: | ---: | ---: |
| 업무시설 | 662 | 111 | 16.8% |
| 공동주택 | 4,800 | 712 | 14.8% |
| 제1종근린생활시설 | 3,145 | 207 | 6.6% |
| 단독주택 | 7,337 | 382 | 5.2% |
| 제2종근린생활시설 | 3,188 | 122 | 3.8% |
| 전체 | 25,336 | 1,787 | 7.1% |

`PROPOSAL` 이 확정률을 서울의 용도 분포에 적용하면 기대값은 다음과 같다.

```text
서울 침수 지하층 건물 9,498동 × 용도별 경북 확정률 = 약 591동
경북 실제 확정 침수 지하주차장                     = 9동
```

**약 591동, 경북의 66배다.** 이는 추정이며 다음 두 방향의 오차를 가진다.

- 과소평가 요인: 서울은 밀도·주차장 설치기준·지가가 경북과 달라 같은 용도라도 지하주차장 비율이 더 높을 수 있다.
- 과대평가 요인: 경북 확정률은 경북 건축물대장으로 측정한 값이므로 서울에 그대로 적용된다는 보장이 없다.

`OPEN` 실제 확정 수는 서울 건축HUB 표제부·층별개요를 수집해야 확인된다. 다만 지도학습 성립 여부를 가르는 규모(수백 동)에는 확실히 도달한다.

## 5. 지하주차장 직접 공공데이터 재확인

`FACT` [전국주차장정보표준데이터](https://www.data.go.kr/data/15012896/standard.do) 18,879건을 전수 확인했다. 지하 여부를 나타내는 필드는 없다.

| 필드 | 값 분포 |
| --- | --- |
| `PRKPLCE_TYPE` | 노외 12,885 / 노상 4,961 / 부설 1,033 |
| `PRKPLCE_SE` | 공영 17,935 / 민영 944 |

`FACT` `지하` 문자열은 주차장명 82건, 지번주소 4건, 특기사항 6건에만 나타난다. 전체의 0.4%다.

`FACT` 따라서 이 자료는 지하주차장 판정에 사용할 수 없다. 대부분 노외·노상 주차장이며 건물 부설 지하주차장이 아니다. **기존의 우회 추론 방식이 여전히 유일한 경로다.**

```text
VWorld A27 (지하층수 >= 1)        → 지하층 보유 건물 후보
→ 건축HUB 표제부 (같은 PNU)        → 옥내주차 면적이 있는 필지로 축소
→ 건축HUB 층별개요 (지하층 용도)    → 용도에 "주차장" 포함 시 확정
```

## 6. 현재 확보 상태와 남은 작업

### 확보 완료

| 자료 | 상태 |
| --- | --- |
| 전국 침수흔적도 38,003건 | `FACT` 확보·검증 완료 |
| 서울 GIS건물통합정보 2026-08-09 | `FACT` 695,761행, 29필드, EPSG:5186 |
| 경북 GIS건물통합정보 2025-12-04 | `FACT` 기존 확보분 |

### 미확보 — VWorld 서버 503

`FACT` 2026-08-22 21시 기준, 서울 1건 성공 후 이어진 모든 다운로드 요청이 HTTP 503을 반환했다. 요청은 서버에 도달했고(데이터셋 다운로드 카운터 증가 확인) 파일만 전달되지 않았다. 클라이언트 문제가 아니라 제공처 측 제한 또는 과부하다.

남은 14개 시도와 우선순위는 다음과 같다. 우선순위는 침수흔적 건수 기준이다.

| 시도 | 침수흔적 | 용량 |
| --- | ---: | ---: |
| 경기도 | 6,730 | 368 MB |
| 충청북도 | 5,218 | 127 MB |
| 경상남도 | 3,806 | 242 MB |
| 전남광주 | 2,919 | 259 MB |
| 강원 | 2,366 | 112 MB |
| 제주 | 2,016 | 59 MB |
| 충청남도 | 1,989 | 194 MB |
| 전북 | 1,329 | 134 MB |
| 부산 | 479 | 80 MB |
| 울산 | 402 | 43 MB |
| 대전 | 171 | 31 MB |
| 인천 | 144 | 52 MB |
| 세종 | 25 | 11 MB |
| 대구 | 5 | 64 MB |

### 아직 구현되지 않은 것

- 서울·전국 건축HUB 수집. 경북만으로 법정동 3,990개 작업이었고 API는 요청당 100행·일일 한도가 있다.
- 전국 표고 특징. Overture·DEM 원본이 로컬에 없어 경북분도 원시 입력부터는 재생성하지 못한다.
- 전국 사건 정의와 강수 결합.
- 전국 학습표와 경북 완전 홀드아웃 평가.

## 7. 그대로 남는 한계

전국으로 넓혀도 다음은 해결되지 않는다. 표본 수 문제와 라벨 성격 문제는 별개다.

- `FACT` 침수흔적도는 **지표면 침수 기록**이다. 지하주차장이 실제로 잠겼다는 직접 기록이 아니다. 건물이 침수 구역 안에 있었다는 대리 신호일 뿐이다.
- `FACT` 침수흔적도는 2022년까지이며 원 제공기관이 갱신을 중단했다.
- `FACT` 2026년 건물 스냅샷으로 2002~2022년 사건을 설명하므로, 사건 당시 존재하지 않았던 건물이 포함된다. `A13 사용승인일자`로 걸러야 한다.
- `OPEN` 서울에서 학습한 패턴이 경북에 그대로 적용되는지는 검증 대상이다. 배수 인프라와 건물 밀도가 다르다. 다만 `주변 대비 고도`처럼 물의 흐름에 직접 관계된 변수는 지역 이전성이 높을 것으로 본다.

## 8. 재현 명령

```bash
./.venv/bin/python scripts/analyze_basement_flood_overlap.py --province 11
./.venv/bin/python scripts/analyze_basement_flood_overlap.py --province 47 \
    --dir data/raw/vworld-buildings/gyeongbuk/AL_D010_47_20251204
```

시도별 요약은 `data/interim/vworld-buildings/basement_flood_overlap_<코드>.json`에 저장된다. 침수 건물의 PNU·좌표 전체 목록은 같은 폴더의 `_flooded.csv`이며, VWorld 원본 속성의 실질적 재배포에 해당하므로 로컬 전용이다.

## 9. 공식 출처

- [VWorld GIS건물통합정보](https://www.vworld.kr/dtmk/dtmk_ntads_s002.do?svcCde=NA&dsId=18)
- [Esri Korea Living Atlas 침수흔적도 미러](https://portal.esrikr.com/arcgis/rest/services/Hosted/Flood_2002_2022/FeatureServer)
- [건축HUB 건축물대장정보 API](https://www.data.go.kr/data/15134735/openapi.do)
- [전국주차장정보표준데이터](https://www.data.go.kr/data/15012896/standard.do)
